# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=rapid_route

# Full connection string alternative (optional)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rapid_route

# JWT Secret
JWT_SECRET=super_secret_rapid_route_jwt_key_2026

# CORS Origin
CORS_ORIGIN=*
